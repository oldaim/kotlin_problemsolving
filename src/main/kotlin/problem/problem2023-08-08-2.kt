package problem

class `problem2023-08-08-2` {
    fun solution(sizes: Array<IntArray>): Int {
        var answer: Int = 0

        val widthArray = sizes.sortBy { it[0] }
        val heightArray = sizes.sortBy { it[1] }



        return answer
    }
}